import { IEnvironment } from './IEnvironment';

export const commonEnvironment: IEnvironment = {
  production: false,
  clientIdentifier: 'starter-app',
  version: '0.0.1',
  apiUrl: 'https://set.prod.api.url.here',
};

